apiVersion: v1
kind: Service
metadata:
  name: techtrends
  labels:
    app: techtrends
spec:
  selector:
    app: techtrends
  ports:
    - protocol: TCP
      port: {{ .Values.servicePort }}
      targetPort: {{ .Values.containerPort }}